@AGENTS.md

## Claude Code

`AGENTS.md` is the primary project instruction source. It is not optional reading.

Before meaningful code changes, follow the preflight and post-change review process it requires. Do not bypass `REVIEW_RULES.md` or the performance guardrails defined there.

If a Claude-specific local convenience conflicts with project rules, prefer project rules.
